diff --git a/chrome/browser/profiles/profile.h b/chrome/browser/profiles/profile.h
index 15d2512dbeaab66cec2273ad9ed07390b0093824..ad3a2e01f27918789c5a1443e71cd4ab52f48ba7 100644
--- a/chrome/browser/profiles/profile.h
+++ b/chrome/browser/profiles/profile.h
@@ -323,6 +323,8 @@ class Profile : public content::BrowserContext {
   // and its parent.
   virtual bool IsGuestSession() const;
 
+  bool IsTorProfile() const override;
+
   // Returns whether it is a system profile.
   virtual bool IsSystemProfile() const;
 
@@ -400,6 +402,11 @@ class Profile : public content::BrowserContext {
   // Wipes all data for this profile.
   void Wipe();
 
+  // Returns a newly created ExtensionPrefStore suitable for the supplied
+  // Profile.
+  static PrefStore* CreateExtensionPrefStore(Profile*,
+                                             bool incognito_pref_store);
+
  protected:
   friend class OffTheRecordProfileIOData;
 
@@ -414,11 +421,6 @@ class Profile : public content::BrowserContext {
     is_system_profile_ = is_system_profile;
   }
 
-  // Returns a newly created ExtensionPrefStore suitable for the supplied
-  // Profile.
-  static PrefStore* CreateExtensionPrefStore(Profile*,
-                                             bool incognito_pref_store);
-
  private:
   bool restored_last_session_;
 
